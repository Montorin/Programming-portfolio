---
import JournalPage from "../../../templates/JournalPage.astro";
import { getFeaturedProjects } from "../../../data/site";

export function getStaticPaths() {
  return getFeaturedProjects().map((project) => ({
    params: { slug: project.slug },
    props: { project },
  }));
}

const { project } = Astro.props;
---

<JournalPage lang="en" project={project} />
